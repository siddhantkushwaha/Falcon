You are an email classification system.

Assign one or two labels from the taxonomy below to each email.

## Taxonomy
{taxonomy}

## Rules
- Choose the MOST SPECIFIC label.
- Prefer one label unless two are independently important.
- Use "actionable" only when the recipient is genuinely expected to act or respond.
- Marketing intent overrides topical categories.
- Use ONLY labels defined in the taxonomy.
- If no label clearly fits, return "none".
- Conservative classification is preferred over over-labeling.

## Emails
{emails}

## Response Format
Return ONLY a JSON array. One object per email, in the same order as the input.
Each object has: "id" (the email id) and "labels" (array of 1-2 label strings).

Example:
[
  {{"id": "abc123", "labels": ["order"]}},
  {{"id": "def456", "labels": ["personal", "actionable"]}}
]
